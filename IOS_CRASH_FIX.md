# إصلاح خطأ mprotect failed على iOS

## المشكلة
```
error: mprotect failed: 13 (Permission denied)
```

هذا الخطأ يحدث عادة عند:
- تشغيل التطبيق على جهاز حقيقي بدون توقيع صحيح
- مشاكل في إعدادات الأمان في iOS
- مشاكل في إعدادات Xcode

## الحلول

### 1. استخدام محاكي iOS (موصى به للتطوير)

**في Xcode:**
1. افتح المشروع: `ios/Runner.xcworkspace`
2. من القائمة العلوية، اختر **محاكي iOS** (مثل iPhone 15 Pro)
3. اضغط `Cmd + R` للتشغيل

**أو من Terminal:**
```bash
# شغّل محاكي
open -a Simulator

# ثم شغّل التطبيق
flutter run -d ios
```

### 2. إعداد التوقيع في Xcode (للأجهزة الحقيقية)

1. افتح `ios/Runner.xcworkspace` في Xcode
2. اختر **Runner** من القائمة الجانبية
3. اذهب إلى **Signing & Capabilities**
4. اختر **Team** الخاص بك (أو "Personal Team" للتطوير)
5. غيّر **Bundle Identifier** إلى شيء فريد (مثل: `com.yourname.educationalapp`)

### 3. تنظيف وإعادة البناء

```bash
cd /Users/kareemfaruq/Downloads/educational-app-ui

# تنظيف
flutter clean
rm -rf ios/Pods ios/Podfile.lock

# إعادة التثبيت
flutter pub get
cd ios && pod install && cd ..

# إعادة البناء
flutter build ios --no-codesign
```

### 4. في Xcode

1. **Product** → **Clean Build Folder** (`Cmd + Shift + K`)
2. **Product** → **Build** (`Cmd + B`)
3. **Product** → **Run** (`Cmd + R`)

## ملاحظات مهمة

- **للتطوير**: استخدم محاكي iOS دائماً - أسرع وأسهل
- **للاختبار**: استخدم جهاز حقيقي مع توقيع صحيح
- **للنشر**: تحتاج Apple Developer Account ($99/سنة)

## إذا استمرت المشكلة

1. تأكد من تحديث Xcode إلى آخر إصدار
2. تأكد من تحديث Flutter: `flutter upgrade`
3. تأكد من تحديث CocoaPods: `sudo gem install cocoapods`
4. أعد تشغيل Xcode

---

**الطريقة الأسهل**: استخدم محاكي iOS للتطوير! 🎯


